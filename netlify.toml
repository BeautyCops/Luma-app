[build]
  command = "npm run build"
  publish = "dist"

# Supabase + GitHub يضيفان غالباً: SUPABASE_URL و SUPABASE_ANON_KEY
# أو عيّني يدوياً: VITE_SUPABASE_URL و VITE_SUPABASE_PUBLISHABLE_KEY

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
