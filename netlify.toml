[build]
  command = "npm run build"
  publish = "dist"

# أضيفي في Netlify → Site configuration → Environment variables:
# VITE_SUPABASE_URL و VITE_SUPABASE_PUBLISHABLE_KEY من مشروعك في Supabase

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
